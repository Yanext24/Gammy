services:
  - type: web
    name: gammy-blog
    env: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: JWT_SECRET
        generateValue: true
    disk:
      name: gammy-data
      mountPath: /data
      sizeGB: 1
